¡Absolutamente! Este sexto video es donde se muestran los **nodos de utilidad** esenciales para manipular y transformar datos, llevando el *workflow* de ejemplo a un nivel práctico.

Aquí tienes la transcripción detallada de los nodos útiles y el progreso del *workflow* que te acercará a ser un experto en n8n:

***

## 🛠️ Nodos Útiles en n8n para Transformación de Datos

El video cubre tres nodos muy comunes que todo constructor de *workflows* profesional utiliza para limpiar, reformatear y estructurar la información.

### 1. Nodo Edit Fields (Set) [[00:41](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=41)]
Este es uno de los nodos más útiles y versátiles.

* **Propósito:** Gestionar y modificar los datos dentro de tus *Items* (filas). Se utiliza para:
    * **Limpiar datos:** Eliminar campos innecesarios para que los *Items* sean más ligeros y fáciles de manejar [[04:10](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=250)].
    * **Añadir o Formatear datos:** Crear nuevos campos o modificar los existentes usando **Expresiones** (ej. concatenar nombre y apellido, o poner el apellido en mayúsculas) [[04:49](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=289)].
* **Configuración clave:** Puedes elegir si deseas **conservar todos los campos de entrada** o si solo deseas **incluir los campos que estás definiendo** en el nodo (*"No input fields"*), lo cual es ideal para limpiar el *Item* por completo [[04:33](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=273)].

### 2. Nodo Aggregate (Agregación) [[01:34](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=94)]
Este nodo pertenece a la categoría de funciones que manejan **múltiples *Items***.

* **Propósito:** Toma múltiples *Items* de entrada y los **combina en un solo *Item*** de salida. Es útil cuando necesitas resumir datos de muchos registros.
* **Uso:** Permite agregar datos de campos específicos (ej. tomar el campo `email` de 10 *Items* y crear una lista con los 10 correos electrónicos dentro de un nuevo *Item* único) [[02:03](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=123)].
* **Nodos Similares:** Existen nodos similares para remover duplicados, limitar el número de *Items* o, a la inversa, desglosar una clave en múltiples *Items* [[02:20](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=140)].

### 3. Nodo Webhook (Trigger) [[02:42](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=162)]
Ya se había introducido, pero aquí se explica su funcionamiento práctico:

* **Propósito:** Activar un *workflow* cuando recibe una **petición HTTP (un *Webhook*)** en una URL específica.
* **URLs:** El nodo proporciona una **URL de Prueba (*Test URL*)** y una **URL de Producción (*Production URL*)**.
* **Flujo:** Cuando un servicio externo (como Stripe o un formulario) envía datos a esa URL, el *workflow* se ejecuta de forma inmediata (*síncronamente*), y los datos recibidos pasan a ser el primer *Item* de entrada del *workflow* [[03:00](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=180)].
* **Buena Práctica:** Puedes **fijar el *Item* de datos** (**Pin Data**) que recibiste durante la prueba para seguir construyendo el *workflow* con ese ejemplo, incluso si recargas la página [[12:27](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=747)].

---

## 🏗️ Construcción del Workflow de Ejemplo

El video continúa el *workflow* de ejemplo (leer datos de Google Sheets, filtrar *Items* con email) y añade lógica avanzada.

### A. Limpieza de Datos (Nodo Edit Fields) [[03:53](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=233)]

1.  Se inserta el nodo **Edit Fields** (Set) inmediatamente después del nodo `Filter` que conservó solo los contactos con email.
2.  Se configura para **no incluir campos de entrada** (*No input fields*) para limpiar los *Items*.
3.  Se añaden y formatean los campos necesarios usando expresiones:
    * `full_name`: Concatenación de **Nombre** y **Apellido** (el apellido se pone en **MAYÚSCULAS** con el método `.toUpperCase()` de JavaScript) [[04:49](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=289)].
    * `email`: Se mapea el email.
    * `company`: Se mapea la compañía.
    * ***Resultado:*** El *Item* resultante es mucho más limpio y solo contiene la información relevante (nombre completo, email, compañía) [[05:30](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=330)].

### B. Opciones de Notificación (Slack)

El *workflow* llega al nodo `IF` que separa los emails personales (Gmail/Hotmail) de los profesionales. Se muestra cómo usar el nodo `Slack` en la rama "Profesional" con dos enfoques:

#### Opción 1: Mensaje Único Agregado (Aggregate) [[06:28](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=388)]
* Se inserta un nodo **Aggregate** después del `IF`.
* El nodo `Aggregate` combina los correos electrónicos y compañías de **todos los *Items*** en un solo *Item*.
* El nodo `Slack` envía **un solo mensaje** a un usuario con la lista consolidada de todos los correos y compañías [[07:41](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=461)].
    * **Ventaja:** Ideal para manejar grandes volúmenes de datos (cientos o miles) sin saturar al usuario con notificaciones individuales [[10:46](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=646)].

#### Opción 2: Mensajes Individuales [[09:31](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=571)]
* Se elimina el nodo `Aggregate`.
* El nodo `Slack` se conecta directamente y se configura con una **expresión dinámica** (ej. `"Nuevo registro de: " + {{ $json.email }} + " de la compañía: " + {{ $json.company }}` ).
* **Resultado:** Como el nodo `Slack` se ejecuta **una vez por cada *Item*** de entrada, se envían múltiples mensajes (uno por cada registro profesional).

### C. Ejemplo de Workflow con Webhook (Lógica de Eventos) [[11:05](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=665)]

Finalmente, el video muestra un *workflow* reactivo que usa un `Webhook Trigger` para manejar diferentes eventos:

1.  **Inicio con Webhook:** Se añade un `Webhook Trigger` para recibir datos con el `método POST`.
2.  **Ramificación Condicional de Eventos:** Se usa un nodo `IF` para comprobar el valor de una clave de entrada (`event`).
    * **Evento 1:** Si el `event` es igual a `"invited_team_member"`, se activa un camino que envía una notificación específica a Slack [[12:53](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=773)].
    * **Evento 2:** Si el `event` es igual a `"account_created"`, se activa otro camino que envía una notificación diferente a Slack [[14:22](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=862)].

Este diseño de *workflow* basado en *Webhooks* e *IFs* es la forma estándar de manejar notificaciones y eventos asíncronos en sistemas automatizados.

***

El próximo video de la serie cubrirá la **gestión de errores** y cómo n8n almacena las ejecuciones pasadas de tus *workflows* [[16:09](http://www.youtube.com/watch?v=Rmi-ckbMOQE&t=969)].
http://googleusercontent.com/youtube_content/10
